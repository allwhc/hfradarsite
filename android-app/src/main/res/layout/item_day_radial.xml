<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="4dp"
    android:gravity="center_vertical"
    android:background="@android:color/white">

    <!-- Day Label Container -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Main Date Label (e.g., 1/12/2025) -->
        <TextView
            android:id="@+id/tvDayLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1/12/2025"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#333333" />

        <!-- Day Info Label (e.g., Day 1 (Mon)) - smaller -->
        <TextView
            android:id="@+id/tvDayInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="Day 1 (Mon)"
            android:textSize="11sp"
            android:textColor="#888888" />

    </LinearLayout>

    <!-- Minus Button -->
    <Button
        android:id="@+id/btnMinus"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:text="-"
        android:textSize="18sp"
        android:textStyle="bold"
        android:background="@drawable/btn_stepper_background"
        android:textColor="#FFFFFF" />

    <!-- Radial Count Dropdown Spinner (24-0, reversed) -->
    <Spinner
        android:id="@+id/spinnerRadialCount"
        android:layout_width="60dp"
        android:layout_height="36dp"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="4dp"
        android:gravity="center"
        android:background="@drawable/spinner_background" />

    <!-- Plus Button -->
    <Button
        android:id="@+id/btnPlus"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:text="+"
        android:textSize="18sp"
        android:textStyle="bold"
        android:background="@drawable/btn_stepper_background"
        android:textColor="#FFFFFF" />

    <!-- Note/Reason Button (visible when radial < 24) -->
    <ImageButton
        android:id="@+id/btnNote"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginStart="4dp"
        android:src="@android:drawable/ic_dialog_info"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:scaleType="fitCenter"
        android:padding="4dp"
        android:contentDescription="Add reason"
        android:visibility="gone" />

</LinearLayout>
